# Festival Queue Display

A simple public queue display for festival attendance management.

## Usage

- Open the page in a browser.
- Press the developer button and enter the password `202690201` to access the admin screen.
- The page automatically reloads once per minute to keep the display fresh.

## Deploy

This project is static and can be deployed to GitHub Pages directly.